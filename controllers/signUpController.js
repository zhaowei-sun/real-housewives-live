const { ModuleFilenameHelpers } = require("webpack");
const { User } = require('../models/model.js');

const signUpController = {};

signUpController.createAccount = (data) => {
    
}

module.exports = signUpController;